var SimpleSegmentAggregation = {

  // Segment an array of events by scale
  aggregate: function(group, scale) {
    scale = scale || 'weeks';

    var aggregates = [];

    _.each(group, function(g) {

    });

    return aggregates;
  }
};
